- name: Run http server
  community.docker.docker_container:
    name: http-server
    image: camillepech/tp-devops-httpd
    state: started
    restart_policy: always
    ports:
      - "80:80"
    networks:
      - name: my_network
# tasks file for roles/proxy
