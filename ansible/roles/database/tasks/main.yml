- name: Run database container
  community.docker.docker_container:
    name: postgres-db
    image: camillepech/tp-devops-postgres
    state: started
    restart_policy: always
    env:
      POSTGRES_USER: "usr"
      POSTGRES_PASSWORD: "pwd"
      POSTGRES_DB: "db"
    networks:
      - name: my_network